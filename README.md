# bg-remov
A simple background removal comparison tool built using HTML, CSS, and JavaScript. It displays the original image and the background-removed version with a draggable slider to compare both images interactively.
